# Mapbox public token — set in Vercel project env vars.
# When this is present, region pages render an interactive Mapbox map.
# When absent, region pages fall back to the hand-drawn SVG placeholders
# under /public/maps/, so local builds still work without a token.
NEXT_PUBLIC_MAPBOX_TOKEN=
